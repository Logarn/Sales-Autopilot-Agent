version: "3.8"
services:
  notifier:
    build: .
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./config:/app/config
      - ./profile:/app/profile
      - ./captures:/app/captures
    restart: unless-stopped
