## Plan Review: Step 3: Documentation and sample

### Verdict: APPROVE

### Summary
The Step 3 plan covers the required outcomes from PROMPT.md: adding a sample capture under `captures/` and documenting the job-detail capture workflow in `README.md`. The scope is appropriately small for a documentation/sample step, and it aligns with the CLI command added in Step 2.

### Issues Found
None.

### Missing Items
- None.

### Suggestions
- Make the sample capture representative enough to exercise the parser fields from Step 1, and ensure the README example uses the actual command (`npm run capture:job -- --file ... [--url ...]`) plus notes that `--url` is needed when the pasted text lacks a URL.
